<div class="col-lg-6 card px-0 {{ class|default }}" style="flex: auto;">
    <div class="card-body d-flex flex-column justify-content-between">
        <div>
            <h5 class="card-title">{{ title }}</h5>
            <p class="card-text">{{ text }}</p>
        </div>
        <a href="{{ path(route) }}" class="btn btn-primary mt-3">Перейти</a>
    </div>
</div>
